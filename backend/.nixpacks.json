{
    "phases": {
        "setup": {
            "nixPkgs": [
                "python312"
            ]
        },
        "install": {
            "cmds": [
                "pip install -r requirements-deploy.txt"
            ]
        },
        "start": {
            "cmd": "uvicorn api_server_v2:app --host 0.0.0.0 --port $PORT"
        }
    }
}